<!DOCTYPE html>
<html>
  <head>
    <title>Портфель "<%= data.portfolio.title %>"</title>
    <link rel='stylesheet' href='/css/style.css' />
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="/popper/popper.min.js"></script>
    <script src="/bootstrap/js/bootstrap.min.js"></script>
  </head>

  <body>
    <header>
        <% include menu %>
    </header>
    
    <main role="main">

        <div class="container-fluid d-flex flex-column wrap">
            
            <section class="row">
                <div class="col-sm-3">
                    <div class="card">
                        <div class="card-body">
                            <div class="card-title">Стоимость</div>
                            <div class="card-text"><%= Number(data.portfolio.cost).toFixed(2) %></div>
                        </div>
                    </div>
                </div>

                <div class="col-sm-3">
                    <div class="card">
                        <div class="card-body">
                            <div class="card-title">Прибыль</div>
                            <div class="card-text"><%= Number(data.portfolio.profit).toFixed(2) %></div>
                        </div>
                    </div>
                </div>

                <div class="col-sm-3">
                    <div class="card">
                        <div class="card-body">
                            <div class="card-title">Годовая доходность</div>
                            <div class="card-text"><%= Number(data.portfolio.annualyield).toFixed(2) %></div>
                        </div>
                    </div>
                </div>

                <div class="col-sm-3">
                    <div class="card">
                        <div class="card-body">
                            <div class="card-title">Изменение за день</div>
                            <div class="card-text"><%= Number(data.portfolio.change).toFixed(2) %></div>
                        </div>
                    </div>
                </div>
            </section>

            <section class="row flex-column wrap">
                <div class="section-title">
                    Состав портфеля по активам
                </div>
                <div class="">
                    <table>
                        <thead>
                            <tr>
                                <th>Актив</th>
                                <th>Текущая стоимость</th>
                                <th>Прибыль</th>
                                <th>Изменение за день</th>
                                <th>Текущая доля</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>Акции</td>
                                <td><%= data.portfolio.shares.cost.toFixed(2) %></td>
                                <td><%= data.portfolio.shares.profit.toFixed(2) %></td>
                                <td><%= data.portfolio.shares.change.toFixed(2) %></td>
                                <td><%= (100 * Number(data.portfolio.shares.cost) / (Number(data.portfolio.shares.cost) + Number(data.portfolio.etf.cost) + Number(data.portfolio.cashe))).toFixed(2) %></td>
                            </tr>
                            <tr>
                                <td>ETF/ПИФ</td>
                                <td><%= data.portfolio.etf.cost.toFixed(2) %></td>
                                <td><%= data.portfolio.etf.profit.toFixed(2) %></td>
                                <td><%= data.portfolio.etf.change.toFixed(2) %></td>
                                <td><%= (100 * Number(data.portfolio.etf.cost) / (Number(data.portfolio.shares.cost) + Number(data.portfolio.etf.cost) + Number(data.portfolio.cashe))).toFixed(2) %></td>
                            </tr>
                            <tr>
                                <td>Рубли</td>
                                <td><%= Number(data.portfolio.cashe).toFixed(2) %></td>
                                <td>0.00</td>
                                <td>0.00</td>
                                <td><%= (100 * Number(data.portfolio.cashe) / (Number(data.portfolio.shares.cost) + Number(data.portfolio.etf.cost) + Number(data.portfolio.cashe))).toFixed(2) %></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </section>

            <section class="row flex-column wrap">
                <div class="section-title">
                    Акции
                </div>
                <div class="">
                    <table>
                        <thead>
                            <tr>
                                <th>Тикер</th>
                                <th>Название</th>
                                <th>Количество</th>
                                <th>Средняя цена</th>
                                <th>Текущая цена</th>
                                <th>Стоимость</th>
                                <th>Курсовая прибыль, %</th>
                                <th>Прибыль</th>
                                <th>Изменение за день, %</th>
                                <th>Доля</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% data.shares.forEach(share => { %>
                                <tr>
                                    <td><%= share.secid %></td>
                                    <td><%= share.shortname %></td>
                                    <td><%= share.amount %></td>
                                    <td><%= share.meanprice.toFixed(2) %></td>
                                    <td><%= share.last.toFixed(2) %></td>
                                    <td><%= share.cost.toFixed(2) %></td>
                                    <td><%= share.exchangeprofitprcnt.toFixed(2) %></td>
                                    <td><%= share.profit.toFixed(2) %></td>
                                    <td><%= share.lasttoprevprice %></td>
                                    <td><%= share.percentage.toFixed(2) %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </section>

            <section class="row flex-column wrap">
                <div class="section-title">
                    ETF/ПИФ
                </div>
                <div class="">
                    <table>
                        <thead>
                            <tr>
                                <th>Тикер</th>
                                <th>Название</th>
                                <th>Количество</th>
                                <th>Средняя цена</th>
                                <th>Текущая цена</th>
                                <th>Стоимость</th>
                                <th>Курсовая прибыль, %</th>
                                <th>Прибыль</th>
                                <th>Изменение за день, %</th>
                                <th>Доля</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% data.etf.forEach(etf => { %>
                                <tr>
                                    <td><%= etf.secid %></td>
                                    <td><%= etf.shortname %></td>
                                    <td><%= etf.amount %></td>
                                    <td><%= etf.meanprice.toFixed(2) %></td>
                                    <td><%= etf.last.toFixed(2) %></td>
                                    <td><%= etf.cost.toFixed(2) %></td>
                                    <td><%= etf.exchangeprofitprcnt.toFixed(2) %></td>
                                    <td><%= etf.profit.toFixed(2) %></td>
                                    <td><%= etf.lasttoprevprice %></td>
                                    <td><%= etf.percentage.toFixed(2) %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </section>
        
        </div>

    </main>

    <footer>
    </footer>

    </body>
    
</html>