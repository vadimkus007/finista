<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

            <!-- Sidebar - Brand -->
    <a class="sidebar-brand d-flex align-items-center justify-content-center" href="index.html">
        <div class="sidebar-brand-icon rotate-n-15">
            <i class="fas fa-laugh-wink"></i>
        </div>
        <div class="sidebar-brand-text mx-3">FINISTA</div>
    </a>

            <!-- Divider -->
    <hr class="sidebar-divider my-0">

            <!-- Nav Item - Favorites -->
    <li class="nav-item <% if (String(blocks.menu).includes('favorites')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/favorites">
            <i class="fas fa-fw fa-star"></i>
            <span>Избранное</span>
        </a>
    </li>

    <!--Nav Item - Quotes -->
    <li class="nav-item <% if (String(blocks.menu).includes('quotes')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/quotes">
            <i class="fas fa-fw fa-chart-area"></i>
            <span>Котировки</span>
        </a>
    </li>

    <!--Nav Item - Portfolios -->
    <li class="nav-item <% if (String(blocks.menu).includes('portfolios')) { %><%= 'active' %><%}%>">
        <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseOne"
            aria-expanded="true" aria-controls="collapseOne">
            <i class="fas fa-fw fa-briefcase"></i>
            <span>Портфель</span>
        </a>
        <div id="collapseOne" class="collapse" aria-labelledby="headingTwo" data-parent="#accordionSidebar">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Свойства портфеля:</h6>
                <a class="collapse-item <% if (String(blocks.menu).includes('actives')) { %><%= 'active' %><%}%>" href="/portfolio/<%= data.portfolio.id %>">Активы</a>
                <a class="collapse-item <% if (String(blocks.menu).includes('trades')) { %><%= 'active' %><%}%>" href="/portfolio/<%= data.portfolio.id %>/trades">Сделки</a>
                <a class="collapse-item <% if (String(blocks.menu).includes('profit')) { %><%= 'active' %><%}%>" href="#">Доходность</a>
                <a class="collapse-item <% if (String(blocks.menu).includes('analytics')) { %><%= 'active' %><%}%>" href="#">Аналитика</a>
            </div>
        </div>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider d-none d-md-block">

    <!-- Sidebar Toggler (Sidebar) -->
    <div class="text-center d-none d-md-inline">
        <button class="rounded-circle border-0" id="sidebarToggle"></button>
    </div>

</ul>