<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

            <!-- Sidebar - Brand -->
    <a class="sidebar-brand d-flex align-items-center justify-content-center" href="index.html">
        <div class="sidebar-brand-icon rotate-n-15">
            <i class="fas fa-laugh-wink"></i>
        </div>
        <div class="sidebar-brand-text mx-3">FINISTA</div>
    </a>

            <!-- Divider -->
    <hr class="sidebar-divider my-0">

            <!-- Nav Item - Favorites -->
    <li class="nav-item <% if (String(blocks.menu).includes('favorites')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/favorites">
            <i class="fas fa-fw fa-star"></i>
            <span>Избранное</span>
        </a>
    </li>

    <!--Nav Item - Quotes -->
    <li class="nav-item <% if (String(blocks.menu).includes('quotes')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/quotes">
            <i class="fas fa-fw fa-chart-area"></i>
            <span>Котировки</span>
        </a>
    </li>

    <!-- Nav Item - Portfolios -->
    <li class="nav-item <% if(String(blocks.menu).includes('portfolios')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/portfolios">
            <i class="fas fa-fw fa-briefcase"></i>
            <span>Выбор портфеля</span></a>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider d-none d-md-block">

    <!-- Heading -->
    <div class="sidebar-heading">
        Портфель
    </div>

    <!--Nav Item - Portfolio Securities -->
    <li class="nav-item <% if(String(blocks.menu).includes('actives')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/portfolio">
            <i class="fas fa-fw fa-file-invoice-dollar"></i>
            <span>Активы</span></a>
    </li>

    <!--Nav Item - Portfolio Trades -->
    <li class="nav-item <% if(String(blocks.menu).includes('trades')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/portfolio/trades">
            <i class="fas fa-fw fa-handshake"></i>
            <span>Сделки</span></a>
    </li>

    <!--Nav Item - Portfolio Profit -->
    <li class="nav-item <% if(String(blocks.menu).includes('profit')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/portfolio/profit">
            <i class="fas fa-fw fa-chart-line"></i>
            <span>Доходность</span></a>
    </li>

    <!--Nav Item - Portfolio Analytics -->
    <li class="nav-item <% if(String(blocks.menu).includes('analytics')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/portfolio/analytics">
            <i class="fas fa-fw fa-chart-pie"></i>
            <span>Аналитика</span></a>
    </li>

    <!--Nav Item - Portfolio Rebalance -->
    <li class="nav-item <% if(String(blocks.menu).includes('rebalance')) { %><%= 'active' %><%}%>">
        <a class="nav-link" href="/portfolio/rebalance">
            <i class="fas fa-fw fa-balance-scale-right"></i>
            <span>Ребалансировка</span></a>
    </li>

    <!-- Divider -->
    <hr class="sidebar-divider d-none d-md-block">

    <!-- Sidebar Toggler (Sidebar) -->
    <div class="text-center d-none d-md-inline">
        <button class="rounded-circle border-0" id="sidebarToggle"></button>
    </div>

</ul>